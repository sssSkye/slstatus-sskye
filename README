slstatus - suckless status
==========================
This is just slstatus configured to work as my status bar.

Requirements
------------
In order to build slstatus you need the Xlib header files.


Installation
------------
Run the following (You might need root access):

    make clean install

